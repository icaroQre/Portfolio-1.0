import styled from "styled-components";

export const UnderlinedContainer = styled.span`

    background-color: ${({ theme }) => theme.tertiary};
    font-weight: 400;
    color: ${({ theme }) => theme.primary};

`;